# pg_hba.conf — DÉVELOPPEMENT LOCAL — trust pour toutes les connexions
# (Acceptable uniquement en développement local)

# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             0.0.0.0/0               trust
host    all             all             ::/0                    trust
